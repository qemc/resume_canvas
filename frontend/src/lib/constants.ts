// A4 at 96 DPI ≈ 794 × 1123 px. We use a round, scaled-down size for standard layout.
export const CANVAS_WIDTH = 794;
export const CANVAS_HEIGHT = 1123;

export const DEFAULT_TEXT_WIDTH = 300;
export const DEFAULT_TEXT_HEIGHT = 80;

export const DEFAULT_IMAGE_WIDTH = 200;
export const DEFAULT_IMAGE_HEIGHT = 200;
