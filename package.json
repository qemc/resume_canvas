{
  "name": "resume-or",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": ".venv/bin/python backend/main.py",
    "build:frontend": "npm run build --workspace=frontend"
  }
}
